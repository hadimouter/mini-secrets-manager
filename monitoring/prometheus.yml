global:
  scrape_interval: 15s

scrape_configs:
  - job_name: mini-secrets-manager
    static_configs:
      - targets: ['app:3000']
    metrics_path: /metrics
    authorization:
      credentials: ${METRICS_TOKEN}
